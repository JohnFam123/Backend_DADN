import getUserLogin from "../model/user.model";
import connectDB from "./database.controller";

function userLogin (){
    client = connectDB();
    // Fetch database data mongo
        //var loginBox = getUserLogin ()
    // Check if user exists

    // Check if password is correct
}

